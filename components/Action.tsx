import { NextPage } from 'next'

interface Props {}

const Action: NextPage<Props> = ({}) => {
  return <div className='w-[20vw] hidden md:block rounded-lg bg-pink-400 mx-auto h-full'>
    
  </div>
}

export default Action